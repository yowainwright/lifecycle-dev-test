name: create pr

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  create-pull-request:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    env:
      PR_NUMBER: ${{ github.event.number }}

    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2.2.4
        with:
          version: latest
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
      - run: pnpm install --no-frozen-lockfile
      - run: TIME=$(date "+%Y.%m.%d-%H.%M.%S"); echo 'New pull request created:' + $TIME >> test.md
      - run: echo 'creating pull request'
      - run: echo ${{ github.event.client_payload.sha }}

      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps

      - name: List env variables
        run: echo $env && echo $PR_NUMBER

      # - name: Run Playwright tests
      #   run: pnpm exec playwright test && echo $PULL_REQUEST_NUMBER
